# Instructions
First, ensure you are in a shell in the intended environment, i.e. the docker container or the VM, with all required packages installed.
To execute the tests in the `test_credential.py` file, run the following command in the `part1` directory:
```
python3 -m pytest test_credential.py
```